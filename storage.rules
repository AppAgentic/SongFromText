rules_version = '2';

// Authenticated read of generated audio + cover art.
// Writes happen server-side via the Admin SDK only.
service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      allow read: if request.auth != null;
      allow write: if false;
    }
  }
}
